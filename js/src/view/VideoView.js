/**
 * Created by Benou on 06/03/15.
 */
( function() {

    var VideoView = Backbone.View.extend( {

    } );

    window.kps = window.kps || { };
    window.kps.VideoView = window.kps.VideoView || VideoView;

} )();